name: Node.js CI

on:
  push:
   branches:
    - master

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - run: npm install
    - run: ng-packagr -p ./package.json
    - run: npm run publish
